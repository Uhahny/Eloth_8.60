-- Stub to satisfy missing reference; does nothing.
function onStepIn(creature, item, position, fromPosition)
    return true
end

function onAddItem(moveitem, tileitem, position)
    return true
end
